import {StyleSheet, Text, View} from 'react-native';

const Home = (): JSX.Element => {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Home;
